refresh_rate: 300 seconds

appenders:
  stdout:
    kind: console
    filters:
      - kind: threshold
        level: info
    encoder:
      pattern: "📋 {d} {l} {t}{n}{m}{n}{n}"

  debug:
    kind: file
    filters:
      - kind: threshold
        level: debug
    path: "./log/debug.log"
    encoder:
      pattern: "📋{d} {l} {t}{n}{m}{n}{n}"

  info:
    kind: file
    filters:
      - kind: threshold
        level: info
    path: "./log/info.log"
    encoder:
      pattern: "📋{d} {l} {t}{n}{m}{n}{n}"

  warn:
    kind: file
    filters:
      - kind: threshold
        level: warn
    path: "./log/warn.log"
    encoder:
      pattern: "📋{d} {l} {t}{n}{m}{n}{n}"

  error:
    kind: file
    filters:
      - kind: threshold
        level: error
    path: "./log/error.log"
    encoder:
      pattern: "📋{d} {l} {t}{n}{m}{n}{n}"

root:
  appenders:
    - stdout
    - debug
    - info
    - warn
    - error
